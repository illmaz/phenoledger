export default function Lineage() {
  return (
    <div style={{
      display: 'flex', flexDirection: 'column', alignItems: 'center',
      justifyContent: 'center', height: 320, textAlign: 'center', gap: 6,
    }}>
      <div style={{ fontSize: 13, fontWeight: 500, color: 'var(--text-2)' }}>Coming in Phase 2</div>
      <div style={{ fontSize: 12, color: 'var(--text-3)' }}>
        Seed lot origin, mother plant records, and full genetic provenance.
      </div>
    </div>
  )
}
